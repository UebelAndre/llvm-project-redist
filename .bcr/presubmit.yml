matrix:
  platform:
  - debian10
  - ubuntu2004
  - macos_arm64
  - windows
  bazel:
  - "7.x"
  - "8.x"
  - "9.x"
tasks:
  run_tests:
    name: Run tests
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    module_path: ""
    build_targets:
      "//..."
    test_targets:
      "//..."
