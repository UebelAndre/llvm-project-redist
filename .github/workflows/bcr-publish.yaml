# name: Publish to BCR

# on:
#   release:
#     types: [published]
#   workflow_dispatch:
#     inputs:
#       tag_name:
#         description: "Release tag to publish (e.g. llvmorg-22.1.4)"
#         required: true
#         type: string

# jobs:
#   publish:
#     uses: bazel-contrib/publish-to-bcr/.github/workflows/publish.yaml@v1.2.0
#     with:
#       tag_name: ${{ inputs.tag_name || github.event.release.tag_name }}
#       registry_fork: UebelAndre/bazel-central-registry
#       tag_prefix: "llvmorg-"
#       attest: false
#       draft: false
#     permissions:
#       contents: write
#       id-token: write
#       attestations: write
#     secrets:
#       publish_token: ${{ secrets.BCR_PUBLISH_TOKEN }}
